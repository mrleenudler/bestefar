====================================================================
PROSJEKT-KONTEKST FOR MODELL ETTER /compact ELLER /clear
Oppdater filen naar ny info dukker opp. Dato: 2026-07-19
====================================================================

LAERDOMMER / FEIL SOM IKKE SKAL GJENTAS (oppdater fortloepende):
- Gradle trenger JAVA_HOME satt til Android Studios JBR i ferske shell:
  $env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
- APK-er distribueres fra dist\-mappen. Etter assembleRelease MAA ny APK
  kopieres til dist\Bestefar-<versjon>.apk — 2026-07-17 fikk brukeren
  gammel APK fordi dist\ ikke ble oppdatert etter bygget.
- Bump versionCode/versionName i android/app/build.gradle.kts ved hver
  distribusjon, saa telefonen viser hvilken versjon som faktisk er innstallert.
- PowerShell 5.1: doble anfoerselstegn INNE i commit-meldinger/argumenter
  til native exe brekker argument-parsing — unngaa " i git commit -m.
- UI-lag (runde 4, v0.6): tema i values/themes.xml (lys brun) + values-night;
  BestefarApp setter nattmodus fra Store.themeMode. ML Kit text-recognition
  brukes til OCR-finpussing av poeng (OcrVerifier.kt) — UKALIBRERT heuristikk,
  maa felttestes. Optikk/ammo/kalkulator er FJERNET (utkommentert m/notat) for
  aa forenkle; OpticProfile-modellen beholdt. Position.BENK fjernet.
- Ikon-portering: plain SVG (viewBox+currentColor) -> _convert_plain_svgs.py;
  potrace-SVG -> _convert_ui_svgs.py (tett bbox). Ikke lag store tomme lerret.
- Toppbaren har naa KUN 3 ikoner (Avstand/Innsikt/Meny). Vaapen->Profil,
  Jakt->Meny, Stilling->kun prompt etter scan. Panels.build() haandterer bare
  avstand + meny; MainActivity.TAB_* konstanter.
- Spec-filer: bestefar_UI_spec-v0-4.md (§12/§13 endringslogg), backend_spec.md,
  til_utvikler_v##.md — oppdater/skriv disse ved store UI-runder.
- Runde 6 (v0.8): Innsikt-matrise nå med ikoner (stilling/vilt) + (i) paa
  tittel; TargetView ringer korrigert (6 hvite/5 sorte); HuntRecord fikk
  created+fellingSuccess; TeamPageActivity (lag-skjelett); RadioGroup krever
  view-id-er for enkeltvalg (jaktmaal-bug); Ui.singleZero()-filter.
  Hovedskjerm ryddet til kun oevelsesskudd+scan.
- Runde 5 (v0.7): Innsikt er en MATRISE (5 vilttyper x jeger-stilling/dyr-
  vinkling/hold), silhuett skalerer med hold, tekst groenn/roed etter jaktmaal.
  Ui.toast() avbryter forrige toast (fiks for toast-koe som blokkerte appen).
  DevTools (flagg ENABLED) i Avanserte innstillinger: Generer serie / Dummy scan.
  Nye fullsides-aktiviteter: LagActivity, AvansertActivity. Venner/Lag = skjelett.
  Tema default LYS. MainActivity.returnToHome=true -> gaa til hovedsiden etter
  Lagre (ikke reaapne meny).

PROSJEKT: Bestefar
Maalet: CV-pipeline for aa score bilder av Kongsberg elektronisk skyteskive (DFS 100m).
Kode ligger i: C:\Users\mrlee\Desktop\Bestefar\

=====================================================================
MOBIL-PORT (2026-07-05/06) - C++-KJERNE VERIFISERT 10/10
=====================================================================
Kravspec: bestefar_CV-kjerne_spec.md (tidl. kravspec.md).
UI-spec: bestefar_UI_spec-v0-4.md. Arkitektur: docs/ARCHITECTURE.md. Layout:
  core/     C++17-port av CV-pipelinen (KUN produksjonsstiene; doedkode som
            Hough-treff/ring_comb_v2/outermost_refine er IKKE portert)
  android/  Kotlin+CameraX+JNI-skjelett (3 skjermer; bygges i Android Studio,
            trenger OpenCV Android SDK, se android/README.md)
  ios/      stub (C-headeren er kontrakten)
  backend/  FastAPI-skjelett: /v1/stats, /v1/failed-analyses, /v1/research
            (forskning strukturelt adskilt, samtykke-gatet, TODO(eier)-felter)

BYGG/VERIFISER C++-KJERNEN (desktop, MSYS2):
  $env:PATH = "C:\tools\msys64\mingw64\bin;" + $env:PATH
  cmake -S core -B core\build -G Ninja -DCMAKE_BUILD_TYPE=Release
  cmake --build core\build
  .venv\Scripts\python.exe verify_cset_cpp.py     # ORACLE for porten: 10/10 PASS
  (pacman-pakker installert: mingw-w64-x86_64-opencv/cmake/ninja)

PORT-LAERDOMMER (IKKE GJENTA FEILSOEKINGEN):
  1. C++ 10/10 kraevde TO dokumenterte avvik fra Python-referansen:
     a) outer_circle: Python subsampler 12000 punkter + 40k stemme-par ->
        senterestimatet er et LOTTERI (+-10px mellom trekk; Pythons numpy-seed
        vinner tilfeldigvis paa alle C-bildene - bevist: Pythons egen vote paa
        et annet subsample bommet 13px). C++: max_edge_points=0 (ALLE punkter)
        + 1M par -> konvergert populasjonsestimat. Fikset C2/C4/C6/C9/C10-
        kalibreringsfeil i ett grep.
     b) NCC-terskler (overlap 0.10->0.25, senter 0.40->0.60) rekalibrert til
        C++-implementasjonens maalte fordeling: ekte C3-overlapp=0.47,
        FP=0.12; ekte C10-sentertreff=0.95, FP=0.40. (Python-fordelingen er
        annerledes pga template-kilde/ramme/OpenCV-versjon.)
  2. Feilsoekingsmetode som funket: dump C++-crop -> kjoer PYTHON-stadier paa
     den -> binaersoek i stadiekjeden. CLI-flagg: --debug, --dumpcrop,
     --outeronly, --votetest. Env: BF_DEBUG_RINGS/OUTER/HITS, BF_VOTE_SEED/PAIRS.
  3. C++-porten fant C10s undertrykte sentertreff (NCC 0.95) som Python-punktet
     "senter-treff i C10 undertrykt" gjaldt - det er naa LOEST i porten.
  4. Ytelse desktop: 2.5-3.3 s/bilde (engangsanalyse, ok for mobil-v1).
  5. FFI: core/include/bestefar/bestefar_ffi.h (ren C). JNI-protokoll =
     flat double[] - holdes i synk med BestefarCore.kt.
  6. Auto-capture: FrameProbe (kontrast-ROI+skarphet+eksponering+dekning) +
     tilstandsmaskin. ALLE terskler UKALIBRERTE (kravspec-krav).

FELTTEST-MILEPAEL (2026-07-07 kveld): FOERSTE EKSAKTE SCORE PAA TELEFON.
  Capture av C4 fra PC-skjerm (naerbilde, bull=0.61): appen ga 5 treff sum 46
  = panel-fasit EKSAKT. Desimaler 10.4/9.8/9.5/9.1/9.0 vs fasit
  10.4/9.8/9.6/9.1/9.0 (en desimal 0.1 av, godt innenfor 0.15-toleransen) -
  TROSS skjerm-moire. Desktop-CLI paa samme JPEG = identisk resultat =>
  on-device-kjernen (JNI/bitmap/rotasjon) har full paritet med desktop.
  Det systematiske +0.3-0.5-desimalskiftet fra tidligere var altsaa REN
  skala-effekt; borte naar skjermen fyller framen.
  Fjern-capture trigget via monitor-artefakten (kabinett-som-bull, kjent
  testspesifikk lekkasje) men ble AVVIST av analysens kvalitetsport
  (avvist2 = ingen poengringer) - forsvarslagene virker: capture-gatene er
  heuristikk, analysegaten er den harde porten.

FELTTEST PAA TELEFON (2026-07-07, paagaar):
  Appen kjoerer paa telefon (16 kB-fiks: CameraX 1.5.1 + compileSdk 36).
  Rapportert: treff mangler / desimalfeil (test = fotografere testbilder fra
  PC-skjerm - moire/glare er mistenkt, MEN ogsaa rotasjonsbug funnet og fikset:
  toBitmap() ignorerer rotationDegrees -> kjernen fikk 90-graders roterte bilder).
  Auto-capture broet etter CameraX 1.5-oppgradering - IKKE loest blindt;
  instrumentert i stedet: eksplisitt YUV_420_888 (1.5-default-mistanke),
  live probe-verdier paa capture-skjermen + logcat-tag BestefarCapture.
  Alle captures lagres naa: JPEG -> Pictures/Bestefar (galleri),
  JSON-sidecar -> Android/data/no.bestefar.app/files/captures/.
  FEILSOEKINGSLOOP: hent bilder fra telefon -> kjoer core\build\bestefar_cli.exe
  paa dem / Python-pipeline -> sammenlign. Neste: bruker tester ny build.

MPI-FIRKANT-BESLUTNING (2026-07-05): IKKE wiret inn i porten. Kravspec §3
deprioriterer den; egne maalinger viser offset<=0.15*delta paa ALLE C-bilder
(inkl. C3 med faktisk skjult treff) => ~0 marginalverdi naa. aux-utvidelses-
punkt i AnalyzeResult tillater ettermontering. Referanse: _vis_mpi_square.py.

VENV - KRITISK (DENNE FELLA KOSTET EN HEL SESJON):
  Bruk ALLTID: .venv\Scripts\python.exe <script>
  IKKE 'python', IKKE 'py', IKKE 'python3' -- paa denne maskinen peker de til
  Microsoft Store-stubben (krasjer med "Python was not found") ELLER til en
  tolk UTEN cv2/scipy ("ModuleNotFoundError: No module named 'cv2'").
  .Bestefar\Scripts\python.exe finnes ogsaa men mangler scipy -> krasjer.
  SYMPTOM naar dette glemmes: du "kjoerer" et vis-script, sier til bruker at
  fila er oppdatert, men INGENTING skrives (kommandoen feilet). Bruker ser den
  GAMLE fila og sier "failed to show an alternate output" / "identical to old".
  Sonnet gikk i denne fella gjentatte ganger i forrige sesjon. Sjekk ALLTID at
  et script faktisk printet forventet output ("Lagret -> ...") foer du melder OK.

NOEKKELFILER:
  config.py          - alle parametere
  Bestefar.py        - analyze_target() hoved-pipeline
  rings.py           - ringkalibrering
  hits.py            - treffdeteksjon
  circles.py         - stemme-detektor (erstatter HoughCircles)
  scoring.py         - poengberegning
  verify_cset.py     - ORACLE: kjoer dette for aa sjekke regrersjon
  hits_truth.txt     - fasit fra Kongsberg-panelene (C1-C10)
  inspect_circles.py - genererer mellomprodukt-montasjer

KJOER ALLTID FOERST (ett kommando, regrsjonstest):
  .venv\Scripts\python.exe verify_cset.py
  Forventer: 8/9 PASS (C10 forkastet = ok, C3 FAIL pga overlappende treff)

NOEKKELPIPELINE:
  foto -> screen.py (finn/rektifiser skjerm) -> rings.py (kalibrering)
  -> circles.py (stemmedeteksjon, HELE skiva) -> scoring.py -> desimalpoeng

DETEKTORBESLUTNINGER (IKKE ANGRE UTEN DATA):
  - hit_unified_detector: True  -- circles.py over HELE skiva, HoughCircles fjernet
  - circ_median_passes: 2       -- enkelt pass klarer ikke LED-rutenettet (testet)
  - circ_pre_gauss_frac: 0.0    -- gauss+1xmedian = like bra paa lette bilder MEN
                                    dropper C3-count fra 8->8 (ikke 10). Ikke antatt
                                    bedre enn 2x median ennaae (ikke implementert)
  - circ_acc_blur_frac: 0.06    -- 0.12 smeltet nabotopper i tette klynger
  - circ_center_exempt_frac: 0.0 -- 1.5 lagde falsk 10-er paa ALLE bilder
  - circ_radial_reject: 0.80    -- NOEDVENDIG; stoerrelsesgating alene stopper ikke
                                    poengringene (bevist med sweep, IKKE endre)

NAEVAERENDE C-SETT STATUS (2026-07-02): 10/10 PASS paa verify_cset.py!
  Alle C1-C10 bestaar antall+sum. C3 9/9, C10 5/5 (mae 0.08). Beste noensinne.
  LOESNINGEN: kant-snapping (screen_refine_gradient_lines) - se SKJERMDETEKSJON.
  MERK: C3-fasiten viser 9 treff (ikke 10) - den fullstendig skjulte 10.5-eren
  er ikke i fasiten, saa 9/9 er full score her.

=====================================================================
SKJERMDETEKSJON & C10 CROP-FEIL (hovedtema siste sesjoner)
=====================================================================
PIPELINE (screen.py, alt paa nedskalert 480px gray, sigma=3.5 blur):
  _apparatus_roi   -> kontrast-ROI (lokalt std-kart) saa segmentering ikke lekker
                      ut i bakgrunn (gress/betong/gjenskinn)
  _screen_blob     -> hysterese-terskling inne i ROI, morph-close, stoerste komp.
  [konveks innhylling]  <- valgfritt, se under
  _rough_quad      -> approxPolyDP -> 4 hjoerner (TL,TR,BR,BL)
  _refine_from_contour -> per side: IRLS-vektet linjetilpasning (Huber init ->
                      Tukey redescending som NULLER uteliggere). Hjoerner =
                      linjeskjaeringer. Krever sterk rett kant ellers forkast.
  rectify_to_screen -> perspektivwarp til rektangel.

C10-PROBLEMET (rotaarsak):
  Blob-en LEKKER ved nedre-venstre hjoerne (BL) inn i apparatkassen/scoreboard.
  De lekkede punktene er en SAMMENHENGENDE stripe (ikke spredte uteliggere), saa
  Tukey klarer ikke nulle dem -> bunn- og venstrelinja skyves UTOVER -> BL-hjoerne
  havner UTENFOR skjermen -> cropen blir skjev -> ringene blir ELLIPTISKE.
  Hvorfor elliptiske ringer dreper senter-voting: sirkelnormaler konvergerer til
  ETT punkt (skarp akkumulatortopp); ellipsenormaler divergerer (kaustikk/evolute,
  bred spredning) og rektangulaere scoreboard-hjoerner samler stemmer paa FALSKE
  topper -> detect_outer_circle laaser paa feil sted -> flat polarprofil -> 0 ringer.

LOESNINGEN PAA C10 (2026-07-02) - KANT-SNAPPING:
  Blob->rough->grov linje er BARE en startgjetning. Etter grov linjetilpasning
  lar _snap_line_to_edge hver side "vandre" mot den ekte skjermkanten:
   - skanner vinkelrett paa linja innenfor et baand
   - per skann-linje: velg det YTTERSTE punktet (lengst fra skjermsenteret) som
     fortsatt er STERK gradient (>= screen_snap_strong_frac * skann-toppen) OG
     peker paa tvers av kanten (retningsport screen_snap_dir_cos)
   - robust re-tilpasning (_fit_line_robust), re-sentreres, 4 iterasjoner
   - sikkerhetsgrense: forkast snap som flytter et hjoerne > 0.25*diagonal
  HVORFOR "ytterste-sterke" og ikke "sterkeste" (argmax): argmax laaste C2/C4 paa
  INDRE sterke gradienter (poengringer/UI-delelinje) -> skjev crop -> ingen ringer.
  Bezelen er utenfor ROI, saa innenfor ROI ER skjermkanten den ytterste sterke.
  Glare ligger utenfor men er diffus/svak -> faller under styrke-gulvet.
  Foerste forsoek (ren argmax) fikset C10 men brakk C2+C4; ytterste-sterke fikset
  alle tre samtidig -> 10/10.
  FLAGG: screen_refine_gradient_lines True, screen_snap_iters 4,
         screen_snap_band_frac 0.045, screen_snap_dir_cos 0.5,
         screen_snap_end_skip_frac 0.08, screen_snap_strong_frac 0.6.
  A/B-probe: _probe_snap_full.py (full pipeline) og _probe_snap_ab.py (crop+sirkel).

FLAGG LAGT TIL (config.py, screen-seksjon ~linje 215):
  screen_blob_open_frac    0.0   Morfologisk aapning paa blob (kutter smale broer).
                                 0.03 knakk C7/C8/C9 (kuttet ekte skjerm). AV.
  screen_side_outer_tol_frac 0.0 Klipp konturpunkter for langt UTENFOR grov side.
                                 0.06 fikser C10 men knekker C1; 0.10 motsatt. Ingen
                                 enkelt verdi funket for baade C10 og C1. AV.
  screen_blob_convex_hull  True  Erstatt blob-kontur med konveks innhylling FOER
                                 rough/refine. Fyller konkaviteten fra den smale
                                 bro-lekkasjen. FIKSER C10 (r=336, riktig senter),
                                 forbedrer C1/C4. MEN C9 har en EKTE konkavitet
                                 (MENY-knapp/moerkt gap) som hull-en fyller feil ->
                                 senter vandrer til (2867,2358) r=1026 = pipeline-feil.
  analyze_screen_fallback  False (var True). True MASKERTE ekte ytelse ved aa falle
                                 tilbake til helbilde naar cropen feilet. Skal vaere
                                 False saa vi ser den ekte cropen. run_cset.py /
                                 verify_cset.py setter den eksplisitt.

GRADIENT-RETNINGS-ALIGN: forsoekt (screen_contract_align_weight) -> forkastet.
  Gradientretning for stoeyete i 480px sigma=3.5-bilde, knakk mange bilder.

OPUS' FORSLAG (musings.txt) - ikke implementert:
  Ikke la ringsenteret vaere gissel for skjermhjoernene. Alternativer:
  (a) kjoer gradient-voting/Hough DIREKTE paa originalen og la den finne beste
      elliptiske senter (konsentriske sirkler -> felles projektivt senter), eller
  (b) bruk den moerke sentrale "10"-skiva som anker (tyngdepunkt av moerk blob) -
      mest robuste enkeltstruktur i bildet. Dette var visstnok vaar gamle fallback;
      bruker spoer hvorfor den ble forlatt (uavklart).

VIS-SCRIPT (Visualiseringer/outputs/):
  _vis_cset_screen.py       BLOB|GROVT|GRADIENT|RAFFINERT|CROP for C1-C10.
       FELLE: dette scriptet kaller _screen_blob/_rough_quad/_refine DIREKTE og
       omgaar detect_screen_quad. Hull-steget maatte derfor DUPLISERES inn i
       make_row (etter panel 1) for at montasjen skal vise hull-effekten.
       Panel 1 viser RAA blob (foer hull); panel 2-5 bruker hull-kontur.
  _vis_cset_outer_circle.py gradient|votemap+sirkel|raw+sirkel paa cropen.
  _vis_c10_ringcalib.py     crop-rute (feil) vs helbilde-rute (riktig) for C10.
  _probe_outer_tol.py       sweeper screen_side_outer_tol_frac over C-settet.

MPI-FIRKANT (midtpunkt-markor) - PAAGAAR 2026-07-04:
  Displayet tegner en liten firkant m/kryss (+) i treffgruppas MPI (mean point of
  impact = tyngdepunkt av ALLE skudd). Invertert overlegg (motsatt farge av bakgrunn).
  Maal: bruk den til aa finne SKJULTE treff: square_senter - tyngdepunkt(detekterte
  treff) peker mot skjult treff (=0 naar alle er funnet).
  Geometri (maalt paa C-sett): side = 53px = 1/sqrt(2)*markordiameter (innskrevet i
  markorsirkel) = 0.58*delta. Strekbredde ~7px = 0.077*delta. Akse-justert i crop.
  DETEKSJON (i _vis_mpi_square.py, config mpi_*): POLARITETS-INVARIANT i gradient-
  domenet (inversjon bevarer kanter). Orientert min(V,H):
    - Rv=|Gx| matches mot 3 vertikale barer (Tv), Rh=|Gy| mot 3 horisontale (Th)
    - score = min(NCC_V, NCC_H)  -> krever BEGGE orienteringer, dreper ring+tall-
      falske topper som vinner paa vertikaler alene
    - presis senter via tyngdepunkt av kolonne/rad-profil (linje-rygger)
  STATUS: alle 10 C-bilder lokaliseres RIKTIG paa tyngdepunktet (d_seed <=17px) med
  VID ROI (mpi_search_r_frac=2.0). Men NCC-score modest (0.24-0.36).
  FUNN (ikke gjenta):
    - Radiell gradient-avstoying (som circles.py) er SKADELIG her: firkantens EGNE
      akse-justerte kanter naer ringsenteret ser radielle ut og undertrykkes ->
      d_seed 200px+. mpi_radial_reject skal vaere False.
    - Plain |grad| NCC (foerste forsoek) ga falske fjern-topper paa C4/C10 ved vid
      ROI; orientert min(V,H) fikset det.
    - Rings er hoved-konkurrenten: konsentriske ringer gir |Gx|/|Gy|-respons som
      delvis matcher bar-templatene (se Cset_mpi_maps.png). Kan ikke fjernes med
      radiell gating. Derfor modest score.
    - C4 (tett gruppe, sloppy crop): riktig lokalisert men lavest score (0.236) og
      LOO-skjoer (hopper 212px naar ett treff droppes).
  FORSOEK 2026-07-05 (approach 1 = strukturell validering): MISLYKTES som
  diskriminator. 3-jevnt-spredte-rygger-i-begge-orienteringer scorer LIKE hoyt
  eller HOEYERE paa TALL (8/9 ved ~1delta radius: mange korte H+V-streker) og
  konsentriske ringer. struct@ekte-firkant 0.22-0.45 overlapper struct@feil-tall
  0.38-0.52. Re-rangering paa struct flyttet deteksjonen til tall -> IKKE bruk
  struct til aa velge kandidat; behold kun som rapportert diagnostikk.
  DET SOM VIRKER: orientert min(V,H) argmax + linje-refine lokaliserer firkanten
  <=17px paa alle 10 (fulle treff). LOO-robust 9/10; C4 (svakest signal comb=0.236,
  tett gruppe) hoppet 212px.
  MAP-PRIOR (mpi_prior_sigma_frac=0.6*delta Gauss om tyngdepunkt): comb*prior ->
  d_seed <=10px alle, C4-LOO fikset (212->2px). MEN: prior gjoer at deteksjonen
  TRACKER tyngdepunktet (LOO-spredning 20-86px paa C2/C5/C6/C7/C9) -> undergraver
  UAVHENGIGHETEN vi trenger for aa maale offset (square-senter minus tyngdepunkt).
  KJERNESPENNING (uloest): firkantens gradient-signal er ISOLERT for svakt/tvetydig
  (comb 0.24-0.36, ikke moenster-separerbart fra tall/ringer). Prior gir robusthet
  men faar detektoren til aa ekko tyngdepunktet -> kan ikke se skjulte treff.
  NESTE (bruker-beslutning): (a) svakt/ingen prior + aksepter C4-skjoerhet, bruk
  offset+comb som confidence; (b) revurder om firkanten er verdt det vs. direkte
  skjult-treff-soek via overlap.py; (c) smalere ROI (~1delta) som mellomting.
  SCRIPT: _vis_mpi_square.py (overlegg Cset_mpi_square.png + mellomsteg
  Cset_mpi_maps.png), _probe_mpi.py (NCC+robusthet-tabell).

GJENVAERENDE ARBEID (prioritert):
  1. LOEST 2026-07-02: C10 CROP via kant-snapping (10/10). Se over.
  2. C3 overlap - morfologisk template-matching for delvis-skjulte treff. Trigges
     kun hvis antall_treff < 10. Maal: finne 10.6. Template = ytre annulus 0.5-1.1*
     marker_r fra isolert rent treff. Se musings.txt.
  3. g10-gauss (gauss_frac=0.10 + 1x median): bruker vurderer, ikke impl.
  4. Fjern dobbel de-skew/kalibrering (screen-crop + perspektivkorreksjon
     overlapper; inspect_hits.py dupliserer prep()).
  5. Forsok circles-detektoren paa HELE maalet (ikke bare ytre ringer).
  6. Senter-treff i C10 er undertrykt - loeses senere.

VIKTIGE FELLER:
  - 'circles' variabel i hits.py skygger over circles-modulen -> 'import circles as circle_det'
  - ring_rho_sigma: 1.2 (fjerne det dropper C3-kalibrering)
  - Real 1.jpg er IKKE lenger referanse/gate (viser ikke full skjerm). Bruk C-sett.
  - console er cp1252 - unngaa non-ASCII (lambda, delta-symbol) i print-setninger
  - Det er TO venvs: .Bestefar\ (ufullstendig) og .venv\ (riktig)

OUTPUT-MAPPE: Visualiseringer/outputs/ (slett ved start av ny sesjon)

UI-RUNDE 7 (v0.9) LÆRDOMMER:
  - addView(view, W, H) LAGER nye LayoutParams og NULLER margin/vekt satt i view.layoutParams.
    Bruk 1-args addView(view) naar du har satt layoutParams (inkl. topMargin) selv.
    (Dette gjorde at topplinjas 5%-margin aldri virket i runde 5/6.)
  - FIT_CENTER i en celle normaliserer allerede SVG-storrelsen. IKKE bruk enum-iconScale
    (ment for MaterialButton.icon) oppaa det -> doblet skalering (liggende bittesmaa,
    sittende overflow). Innsikt har egne skalaer.
  - Aa skalere en HEL ImageButton med bakgrunn/ramme (scaleX/Y) skalerer ogsaa rammen ->
    "loddrette striper". Legg ramme paa en FrameLayout av fast storrelse, skaler kun bildet.
  - Naar bruker sier "X ble ikke implementert" men koden finnes: sjekk om (a) de testet
    eldre APK, eller (b) feltet bare vises betinget (f.eks. snitt vises kun med historikk;
    "med anlegg" krever valgt hjelpemiddel). Legg til manglende bit (her: "Poeng:" foran
    TOTALEN, som var en annen visning enn "Poeng:"-lista til hoyre for skiva).
  - Dato-"bytte"-animasjon: ts har HH:mm og er alltid ulik. Bruk DAG (toLocalDate) som
    noekkel for aa trigge animasjon, ikke hele tidsstempelet.

UI-RUNDE 8 (v0.10) LAERDOMMER:
  - ROTAARSAK (kostet 2-3 runder): Ui.themeColor returnerte tv.data RAATT.
    android.R.attr.textColorPrimary oppløses til en ColorStateList-REFERANSE, ikke en
    farge-int -> tv.data er en RESSURS-ID (som ARGB = tilfeldig/usynlig farge). Derfor
    var ALT som ble tintet med tekstfargen "usynlig" i baade lys og moerk (vilt-
    silhuetter, valgt piktogram, viltnavn-tekst, jaktlogg-silhuetter). colorPrimary
    virket fordi den oppløses til direkte farge-int. Fiks: sjekk tv.type (COLOR_INT-
    range) -> tv.data; ellers tv.resourceId -> ContextCompat.getColor / (fallback)
    ResourcesCompat.getColorStateList(...).defaultColor. NAAR noe tintet med tekst-
    fargen er "usynlig" uansett natt/dag: MISTENK theme-attr-oppløsning FOERST.
  - MaterialButton.iconSize tvinger ikonet til KVADRAT (iconSize x iconSize) og klemmer
    brede ikoner (liggende) flate. Vil du beholde aspekt: bruk en ImageView med
    FIT_CENTER i en boks (fast hoyde, variabel/fast bredde), ikke b.icon+iconSize.
  - "## skudd" i stillingsvelgeren viste SERIER (seriesCountByPosition), ikke skudd.
    La til shotsCountByPosition() (sum av shots.size). Sjekk alltid at en teller faktisk
    teller det etiketten sier.
  - Tema-attr som er ColorStateList (textColorPrimary) vs farge-int (colorPrimary):
    de oppfoerer seg ULIKT i resolveAttribute. Ikke anta at alle attr gir farge-int.
  - Checkbox-default: "Felling var vellykket" skal vaere UAVKRYSSET slik at et bevisst
    valg tvinges frem via bekreftelsesdialog. Standard-checked skjuler feillogging.
  - Oppstartspopups som brukeren "ikke vil se hver gang": flytt valget til Avanserte
    innstillinger (bryter), la popupen kun styres av dev-flagg. Bruker vil ha KONTROLL,
    ikke gjentatte spoersmaal.

UI-RUNDE 9 (v0.11) LAERDOMMER:
  - "Moerk silhuett skal vaere tekstfarget" viste seg aa bety den VARME LYSEBRUNE
    (colorPrimary_dark = #D8B79B), ikke android:textColorPrimary. Naar bruker sier
    "tekstfarget/varm" om et brunt tema: bruk colorPrimary, ikke tekstfargen.
  - Plain-SVG (viewBox + <path fill=currentColor>) -> vector drawable: viewport =
    (vb.w, vb.h), <group translateX=-vb.minx translateY=-vb.miny>, fill #FF000000
    (tintes i UI). Se _convert_species_svgs.py. Elg/Villsvin-silhuetter finnes i UI/.
  - Innsikt-ramme: for aa faa hold-kolonnen RETT under staaende, laas matrisebredden
    til 3*(cellW+gap) (tre foerste kolonner). Da lander 4. kolonne (hold) under
    staaende. Ikke bruk weight=1 paa matrisen hvis noe skal staa i "kolonne 4".
    "Smal stripe mellom ikoner" = 2 dp marginEnd/bottomMargin/topMargin overalt.
  - "Ikke mulig aa skrive inn" krever isEnabled=false OG isFocusable=false +
    isFocusableInTouchMode=false (kun isEnabled kan i praksis fremdeles fokuseres i
    enkelte tilfeller). Sett dem UMIDDELBART, foer evt. animasjon.
  - Flytende knapp over ScrollView (FrameLayout-overlay) = innhold scroller BAK
    knappen. Vil bruker at innholdet skal KLIPPES over knappen: gi ScrollView
    bottomMargin (ikke padding) = knappehoyde; ScrollView klipper egne barn til
    grensene sine.
  - MaterialButton.iconSize (runde 8) og whole-button-scale (runde 7) er begge felle
    for ikon-forvrengning/rammeskade — bruk ImageView+FIT_CENTER i egen boks.

CAPTURE-RUNDE (2026-07-31, v0.12) — FELTTEST SKYTEBANEN (musings.txt):
  - CAPTURE-FIRST: bildet tas STILLE naar kriteriene er oppfylt; UI («Klar!»-
    groenn ramme 0,4 s -> blits) spilles ETTERPAA. stability_frames 24 -> 6
    (24 fantes kun fordi gammel flyt gloedet FOER capture; ett daarlig frame
    nullstilte vinduet -> "altfor vanskelig aa faa scannet").
  - OPP-NED-ROTAARSAK: sensorLandscape roterer 180 grader UTEN activity-
    recreate -> ImageCapture.targetRotation blir stale -> feil rotationDegrees.
    Fiks: OrientationEventListener som setter targetRotation kontinuerlig.
    (Preview haandteres av PreviewView selv; KUN stillbildet rammes.)
  - ScanFrameView (erstatter scan_frame*.xml-drawables, som er SLETTET):
    sirkel om hvit skive + bull, poengliste- og oppsummeringsrektangel,
    9dp ytre ramme, matt 0x66FFFFFF (groenn 0xCC4CAF50 ved klar). Geometri
    MAALT paa rektifiserte C-crop (_probe_frame_geometry*.py): senter
    (0.415,0.420), r_hvit=0.304*B, r_bull=0.121*B, tabell x 0.752-0.990,
    liste y 0.016-0.516, oppsummering y 0.516-0.824. Verifikasjonsbilde:
    Visualiseringer/outputs/scan_frame_mock.png (_vis_scan_frame_mock.py -
    holdes i synk med ScanFrameView.kt). NB: C5-bull maalte 0.090 (treff-
    markoerer brakk komponenten); C1/C9 = 0.121 brukt.
  - GLARE-METRIKK i frame_probe: stoerste sammenhengende mettede (>=250)
    komponent i skjermblob / blob-areal, morf-aapning 5x5 mot punktmetning.
    max_glare_frac=0.05 UKALIBRERT, inngaar i quality-gaten + eget statushint.
    FFI: BfFrameProbe fikk glare_frac+glare_ok, BfAutoCaptureParams fikk
    max_glare_frac; JNI-protokollen er naa 15 doubles (var 13) - jni_bridge.cpp
    OG BestefarCore.kt maa endres i takt.
  - Capture-statustekster: status_searching/closer/hold_still/capturing
    ERSTATTET av status_compose/glare/ready (+ status_quality beholdt).
  - verify_cset_cpp.py etter endringene: 10/10 PASS (analysen uberoert).
  - FELLE (Edit-verktoey): CLAUDE_CONTEXT.txt har blandet tegnsett; norske
    tegn i eldre avsnitt matcher ikke alltid — bruk ASCII-ankre ved redigering.

UI-RUNDE 10 (v0.13) LAERDOMMER:
  - statusBarColor VIRKER IKKE naar targetSdk >= 35: edge-to-edge tvinges og
    fargen ignoreres. Vil du ha svart systemlinje maa du ogsaa sette
    android:windowOptOutEdgeToEdgeEnforcement=true (API 35, krever tools:
    targetApi og xmlns:tools i <resources>). Bonus: da blir Ui.applyInsets en
    no-op av seg selv (insets = 0), saa ingen layout maa endres.
  - "Skjermen hopper til toppen" ved avkryssing = full re-render av ScrollView.
    Behold rad-viewene som felt og oppdater KUN bakgrunn/synlighet. Knapper som
    bytter (soppelboette/Avbryt/Lukk) skal alltid ligge i treet med
    visibility-toggle, ikke legges til/fjernes.
  - rRel er i RINGSTEG, og ett ringsteg = ett poeng (DevTools: rRel = 10.9-dec).
    "Margin <= 0,1 poeng" mellom treffpunkter = euklidsk avstand <= 0.1 i
    (rRel*cos t, rRel*sin t). Like POENG alene kan ikke skille et opp-ned bilde
    fra en ekte duplikatserie - radiene er identiske, punktene er speilvendte.
  - Skal noe staa "rett til hoyre for" den nederste raden i en ramme: den raden
    maa ligge INNE i den samme vertikale kolonnen, ikke under hele body-en.
    (Innsikt: vinkelraden flyttet inn i venstrekolonnen som rad 6, saa
    hold-kolonnens 6. knapp (200 m) lander ved siden av den.)
  - "Alt skal passe paa en skjerm" => regn radhoyden ut fra
    displayMetrics.heightPixels minus et reservert chrome-budsjett, og klem den
    (coerceIn). Ikke hardkod dp naar innholdet har fast antall rader.
  - View-animasjon som flytter et barn UT av sin egen rad blir KLIPPET.
    clipChildren=false + clipToPadding=false paa alle foreldre (ogsaa
    NestedScrollView i dialogen), ellers ser man ingenting.
  - OcrVerifier returnerte alltid OCR-poengene i skjermrekkefolge; det var
    VISNINGEN i ResultActivity som sorterte (ocr.sorted()). Sjekk hvor
    sorteringen faktisk skjer for du "fikser" datalaget.
  - Funksjonalitet som skal PAUSES (forskning) - bruk ett navngitt flagg
    (Dialogs.RESEARCH_ENABLED) som alle inngangene sjekker, ikke slett kode.

BACKEND-RUNDE 1 (2026-08-02, fase 1 i Plan_for_backend_implementering.odt):
  - Kilder: backend_spec.md + Plan_for_backend_implementering.odt (faseplan
    0-8) + UI/musings_backend.txt (kontoer som er opprettet).
  - INFRA: Fly.io-app "bestefar-api", org personal, region ams ->
    https://bestefar-api.fly.dev  (eget domene ikke kjoept ennaa).
    DB = Supabase-prosjekt Bestefar_base (ref puioipitfilhqnarrsup).
    Objektlagring = Cloudflare R2 (ikke koblet inn ennaa, fase 6).
  - EGEN VENV FOR BACKEND: backend\.venv (fastapi/sqlalchemy/psycopg).
    Prosjektets .venv er CV-pipelinen (cv2/scipy) - IKKE bland dem.
    Kjoer: backend\.venv\Scripts\python.exe -m pytest tests -q
  - FELLE (flyctl): `flyctl deploy --config backend/fly.toml ./backend` feiler
    med "config is missing an app name" - --config tolkes relativt til
    app-katalogen. Kjoer flyctl deploy FRA backend\ i stedet (det er ogsaa
    det .github/workflows/deploy-backend.yml gjoer).
  - Engine lages LATT (backend/app/db.py), ikke ved import: ellers doer
    prosessen ved oppstart hvis DB er nede, og Fly gir en deploy-loop uten
    forklaring. /health svarer alltid 200; DB-tilstand staar i kroppen.
  - /health rapporterer "feilkonfigurert" hvis ENV=prod og DATABASE_URL er
    sqlite - ellers ville SELECT 1 gitt "ok" paa en flyktig containerfil.
  - E-post (§10) er leverandoer-agnostisk (services/mailer.py): Resend hvis
    RESEND_API_KEY, ellers SMTP hvis SMTP_HOST, ellers kun logging. Meldingen
    lagres ALLTID i DB foerst, videresending er en bakgrunnsoppgave som kan
    feile uten datatap (forward_error paa raden).
  - Secrets: settes med `flyctl secrets set ... -a bestefar-api`. La BRUKEREN
    kjoere kommandoen (passord skal ikke inn i chat-loggen).

BACKEND-RUNDE 2 (2026-08-02/03, fase 2 - datamodell + Alembic):
  - app/models.py er SPLITTET til pakken app/models/ (base, user, training,
    social, ops, research). __init__ re-eksporterer alt, saa
    `from ..models import X` virker som foer.
  - 23 tabeller i Supabase. Forskning ligger i EGET Postgres-skjema
    (research.consents/records/deletion_requests) uten FK til brukertabellene.
    SQLite har ikke skjemaer -> schema_translate_map {research: None} i
    app/db.py OG migrations/env.py.
  - Forsknings-pseudonym AVLEDES (HMAC-SHA256(secret, user_id)), det lagres
    ikke i en tabell - en oppslagstabell ville vaert den reversible koblingen
    spec §7 forbyr. Secret kan derfor ikke roteres uten aa brekke koblingen.
  - Enum-kolonner: native_enum=False (VARCHAR+CHECK). Postgres-native ENUM
    krever ALTER TYPE-migrasjoner og finnes per skjema - ikke verdt det.
  - Serie-ID = klientens egen UUID -> PUT er idempotent (klienten koer usendte
    serier og kan sende samme serie flere ganger).
  - FELLE (SQLAlchemy): `series.shots.clear()` + append i samme flush gir
    UNIQUE-brudd - INSERT legges FOER DELETE. Maa `s.flush()` etter clear().
  - FELLE (float): sum(10.4, 9.8) = 20.200000000000003. Desimalpoeng har én
    desimal -> round(sum, 1) ved lagring.
  - FELLE (tester): Alembics env.py importerer app.config og varmer opp
    lru_cache-en i settings(). conftest maa nullstille sys.modules ETTER
    `alembic upgrade head`, ellers ser testene gamle miljoevariabler.
  - Testene bygger basen med Alembic (ikke create_all), og
    tests/test_migrations.py sammenligner modeller mot base -> modellendring
    uten migrasjon feiler i CI. Den sjekken kan BARE kjoere paa Postgres
    (schema_translate_map gjoer research-skjemaet usynlig for refleksjon paa
    SQLite); CI kjoerer derfor suiten to ganger, SQLite + Postgres-service.
  - fly.toml [deploy] release_command = "alembic upgrade head": migrasjonen
    kjoerer i egen maskin FOER ny versjon slippes til. Dockerfile maa derfor
    ogsaa kopiere alembic.ini + migrations/.
  - Auth mangler til fase 3: deps.current_user svarer 501 i prod og godtar
    headeren X-Debug-User-Id kun naar ENV != prod.

BACKEND-RUNDE 3 (2026-08-03, fase 4 - backup/sync §2):
  - PUT/GET /v1/backup + GET /v1/backup/meta + DELETE. Bloben er
    KLIENT-KRYPTERT; serveren lagrer bytes den ikke kan lese.
  - Raa application/octet-stream med metadata som query-parametere (ikke
    base64/JSON) - sparer ~33 % paa vaar stoerste nyttelast. Grense 16 MB.
  - VIKTIG DESIGNVALG: serveren avviser PUT der client_ts er ELDRE enn den
    lagrede (409, med ?force=true som overstyring). Spec §2 sier
    last-write-wins per post-ID, men den regelen kan bare haandheves
    klient-side - serveren ser ikke inn i den krypterte bloben. Uten vernet
    kunne en telefon som synker foerste gang paa maaneder viske ut alt.
    Likt client_ts godtas (retry etter avbrutt kall skal ikke feile).
  - /v1/backup/meta finnes for at «har jeg noe aa gjenopprette?» paa ny
    telefon ikke skal kreve nedlasting av hele bloben.
  - Ingen ny migrasjon: Backup-tabellen laa allerede i initial-migrasjonen.

BACKEND-RUNDE 4 (2026-08-03, fase 5 - venner §3/§3.1):
  - Nye ruter: GET/PUT /v1/profile (+/sharing), GET /v1/users/search,
    POST /v1/friends/request|respond, GET /v1/friends(/requests),
    DELETE /v1/friends/{id}.
  - TIDSSONE-FELLA (kostet en runde, ikke gjenta): utcnow() gir AWARE
    datetime, men en naiv DateTime-kolonne leverer NAIVE tilbake. Alt som
    SAMMENLIGNER ny verdi mot lagret kaster «can't subtract offset-naive and
    offset-aware datetimes» - og bare paa de kodestiene som sammenligner, saa
    det dukker opp tilfeldig senere. SQLite gjoer det verre: den leverer naive
    verdier selv med timezone=True. LOEST med UtcDateTime (TypeDecorator) i
    models/base.py + type_annotation_map paa Base, saa alle Mapped[datetime]
    faar den automatisk. mapped_column(DateTime, ...) skal IKKE oppgis lenger.
    models.as_utc() normaliserer innkommende naive verdier (query-parametere).
    Migrasjon 4ef8ebf137fc konverterer til timestamptz; no-op paa SQLite.
  - Autogenerate KAN IKKE brukes for typeendringer her: den maa kjoere mot
    Postgres for aa se forskjellen, og mot SQLite produserer den bare stoey
    fra schema_translate_map (rapporterer research-tabellene som slettet).
    Migrasjonen ble skrevet for haand med kolonnelista generert fra
    Base.metadata.
  - Soek: KUN eksakt treff paa public_id eller telefon, kun findable-brukere.
    Ingen fritekstsoek paa navn - det ville gjort brukerbasen listbar.
    Sjekksifferfeil i ID gir 422 UTEN oppslag og teller ikke som bom.
  - Karantene (§3.1) ligger i DATABASEN (services/quarantine.py), ikke i
    minnet som ratelimit.py: den skal overleve omstart og gjelde paa tvers av
    Flys to maskiner. Bare BOM telles; treff er normal bruk.
  - Utgaaende filtrering skjer SERVER-side (services/sharing.py). Da er
    «deaktivering nuller delte felt» en garanti, ikke en klientdetalj.
  - FUNN: Friend.kills[] i spec §3 kan IKKE leveres - jaktloggen ligger inne i
    den klient-krypterte backup-bloben (§2), som serveren ikke kan lese.
    Krever spec-avklaring (egne jaktrader), ikke bare implementasjon.

BACKEND-RUNDE 5 (2026-08-03, fase 6 - lag §4/§11):
  - Nye ruter i routers/teams.py + messages.py: opprett/list/naer, rename,
    invite, join, medlemmer, lederskap m/bekreftelse, avstemning, utfordring
    av inaktiv leder, meldingskoe.
  - FELLE (FastAPI-ruting): /{team_id}/leaders/confirm ble spist av
    /{team_id}/leaders/{member_id} (member_id="confirm") -> 403 i stedet for
    404. FastAPI tar FOERSTE rute som matcher; spesifikk sti MAA deklareres
    foer parametrisert.
  - LAT AVGJOERELSE av frister (services/teamgov.py): baade avstemning (7 d)
    og inaktiv-leder-utfordring (7 d) avgjoeres foerste gang noen SPOER, ikke
    av en bakgrunnsjobb. Ingen scheduler aa drifte. Naar push (fase 8) kommer,
    boer et periodisk kall likevel inn saa varselet gaar paa fristen.
  - deps.touch() oppdaterer User.last_seen_at maks én gang per time - §11
    trenger «har leder brukt appen siste maaned?».
  - Enstemmighet avslutter avstemningen tidlig; uavgjort ved frist gir
    outcome=expired (ingen leder kaares paa terningkast).
  - SMS-invitasjon: ingen leverandoer (utsatt til v2), saa vi returnerer
    delivery_status=failed MED lenken, slik at klienten kan dele den via
    ACTION_SEND. E-post sendes via mailer.
  - /i/{token}-redirecten svarer 302 til butikken UANSETT om tokenet finnes -
    lenken deles i aapne kanaler og skal ikke kunne brukes til aa sjekke
    hvilke lag som eksisterer.
  - Ingen ny migrasjon: alle lag-tabellene laa i initial-migrasjonen.

FELLE - SQLITE HAANDHEVER IKKE FREMMEDNOEKLER (2026-08-03, kostet en CI-runde):
  82 av 105 tester feilet i CI mot Postgres mens ALLE passerte paa SQLite.
  Aarsak: SQLAlchemy satte inn sharing_preferences FOER users. Det finnes
  ingen relationship() mellom User og SharingPreference, og uten en slik
  kobling har unit-of-work ingen rekkefoelge aa sortere etter - FK-kolonnen
  alene er ikke nok. SQLite sa ingenting fordi PRAGMA foreign_keys er AV som
  standard.
  TO FIKSER, begge noedvendige:
    1. db.py slaar paa PRAGMA foreign_keys=ON for SQLite, saa feilklassen er
       synlig lokalt i stedet for aa dukke opp i CI.
    2. s.flush() etter s.add(parent) foer barnet legges til (deps.py).
  REGEL: legger du til en forelder og et barn i samme flush UTEN en
  relationship() mellom dem, MAA du flushe imellom.

FELLE - CI-LOGGER KAN IKKE LESES ANONYMT: GitHub svarer 403 paa
  /actions/jobs/<id>/logs uten token, ogsaa for offentlige repoer. Jobbnavn og
  stegstatus er derimot aapent i /actions/runs. Trenger du selve loggen, be
  brukeren eksportere den (de kan lagre den i repoet som job-logs.txt).

FELLE (flyctl, 2026-08-03): ~/.fly/config.yml ble skrevet full av NUL-bytes
  (1929 byte) da to instanser kjoerte flyctl samtidig -> «yaml: control
  characters are not allowed» paa ALLE flyctl-kommandoer. Filen holder
  access-tokenet og kan ikke repareres; krever `flyctl auth login` paa nytt.
  Unngaa aa kjoere flyctl fra to instanser samtidig.

UI-RUNDE 11 (v0.14) - BACKEND-KOBLING, KLIENTSIDEN:
  - Appen hadde NULL nettverkskode for denne runden. Nye filer: Api.kt
    (HttpURLConnection, ingen nytt bibliotek) og Sync.kt (filbasert koe i
    filesDir/dev_uploads). Naar auth kommer er det eneste nye en token-header
    i Api.request() - ikke et helt transportlag.
  - BuildConfig er AV som standard fra AGP 8. buildFeatures { buildConfig =
    true } MAA settes for at buildConfigField skal kompilere.
  - Klartekst-HTTP (http://10.0.2.2:8000 mot lokal backend) krever
    usesCleartextTraffic. Legg den i src/debug/AndroidManifest.xml, IKKE i
    hovedmanifestet - release skal bare snakke HTTPS. tools:replace trengs
    ikke naar hovedmanifestet ikke setter attributtet.
  - FELLE SOM NESTEN BLE BEGAATT: applyScores() overskriver record.shots med
    OCR-poengene FOER donasjonen koees. En "ocr_match"-donasjon ville da hatt
    OCR-poengene i BEGGE felt = null informasjon om hva kjernen saa. Ta vare
    paa kjernens poeng i et eget felt (detectedScores) ved onCreate.
  - Tre plassholdere i UI-et LOEY foer denne runden: queueDevImage() skrev
    filer ingen sendte, "N usendte opplastinger" var en teller som kun kunne
    vokse, og "Send bildet til feilanalyse" viste kvittering uten aa sende.
    Naar en teller/knapp gjelder backend: sjekk at det finnes en sender.
  - Feilklassifisering i en offline-foerst-app: retryable = code 0 (naadde
    aldri fram) / 408 / 429 / >=500. Alt annet (400/413/422) er PERMANENT og
    elementet skal kastes, ellers gror koeen med soppel i evig tid.
  - /v1/feedback og /v1/failed-analyses krever IKKE innlogging (bevisst, se
    docstringene). /v1/stats gjoer (deps.current_user) - derfor er
    SeriesRecord.uploaded fortsatt uroert.
  - Verifisert mot produksjon 2026-08-02 med noeyaktig klientens wire-form:
    /health 200, /v1/feedback 202, /v1/failed-analyses 201. To testrader
    (id 1 i begge tabeller) staar i Supabase og kan slettes.
  - core_version i donasjonene er appens versionName; CV-kjernen eksponerer
    ingen versjon over FFI. Aapent spoersmaal til eier (til_utvikler_v014.md).
  - Backend-agenten jobbet i samme repo SAMTIDIG (backend/app/models.py ble
    til pakken backend/app/models/, og backend_spec.md ble skrevet av begge).
    Rediger kun android/ + egne docs-seksjoner naar to instanser kjoerer.
